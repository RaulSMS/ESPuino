# CLAUDE.md

@AGENTS.md

## Claude Code

No Claude-specific overrides beyond what's in `AGENTS.md` right now. Claude Code's own auto-memory
system (`~/.claude/projects/<project>/memory/`) supplements this with session-learned facts — don't
duplicate those here or in `AGENTS.md`.
